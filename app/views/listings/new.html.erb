<%= simple_form_for @listing, :html => { :multipart => true } do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.file_format :photo, :label => 'Your beautiful boat' %>
    <%= f.button :submit,
                 'Upload File',
                 class: 'btn' %>
<% end %>